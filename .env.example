# URL da API em produção (em desenvolvimento, o proxy do Vite redireciona /api para localhost:3001)
VITE_API_URL=https://amacor.cloud

# Token de autenticação da API do VPS (Bearer token)
VITE_API_TOKEN=
